using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace CareerCup150.Chapter19_Medium
{
    public class _19_06_NumberToWord
    {
        /*
         * Given an integer between 0 and 999,999, print an English phrase that describes the integer (eg, “One Thousand, Two Hundred and Thirty Four”).
         */
        // On second thought, I plan not to implement this method.
        // it is a tedious and simple problem... not that technical.
        //public string NumberToWord(int input)
        //{
        //    int[] digits = new int[6];
        //    for (int i = 0; i < 6; i++)
        //    {
        //        digits[i] = input % 10;
        //        input = input / 10;
        //    }

        //    StringBuilder sb = new StringBuilder();
        //    for (int j = 3; j > 0; j = j - 3)
        //    {
        //        for (int i = 2; i > 0; i--)
        //        {
        //            if (digits[i + j] > 0 && i ==2)
        //            {
        //                sb.Append(digits[i + j].ToString() + " Hundred ");
        //            }
        //            if (digits[i + j] > 0 && i == 1)
        //            {
        //                switch (digits[i + j])
        //                {
        //                    //case 1:
        //                    //    sb.Append("and Ten
        //                }
        //            }
        //        }
        //    }

        //}
    }
}
