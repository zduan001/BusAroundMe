using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace CareerCup150.Chapter5_BitOperation
{
    class _05_01_BitWise
    {
        /*
         * You are given two 32-bit numbers, N and M, and two bit positions, i and j. 
         * Write a method to set all bits between i and j in N equal to M (e.g., M 
         * becomes a substring of N located at i and starting at j).
                EXAMPLE:
                Input: N = 10000000000, M = 10101, i = 2, j = 6
                Output: N = 10001010100
         */
        // I will do bit operation question last, since they are not very often show up on my interviews.
        public int SetBits(int n, int m, int start, int end)
        {
            //int tmp = 0;
            //for (int i = 0; i < 32; i++)
            //{
            //    tmp = tmp << 1;
            //    if (!(i > 32 - j && i < 32 - i))
            //    {
            //        tmp = tmp + 1;
            //    }
            //}
            return -1;


        }

    }
}
